{
  "name": "lean-ir-wasi-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "export:ir": "python3 scripts/export_ir.py",
    "install:wasi": "bash scripts/install-wasi-sdk.sh",
    "build:wat": "bash scripts/build-wat.sh",
    "build:wasi": "bash scripts/build-wasi.sh",
    "build:interp": "bash scripts/build-interp.sh",
    "build": "npm run export:ir && npm run build:wat",
    "test": "npm run build && node scripts/smoke_node.mjs",
    "test:wasi": "npm run export:ir && npm run build:wasi && node scripts/smoke_node.mjs",
    "test:interp": "npm run export:ir && npm run build:interp && node scripts/smoke_node.mjs",
    "dev": "vite web --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^8.0.13",
    "wabt": "^1.0.39"
  }
}
